info:
  name: 05 Get Order - Not Found
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/orders/ORD-FAKE999"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // BUG TEST: ต้องได้ 404 แต่ Lek ส่ง 200 กลับมา
        expect(res.status).to.equal(404);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
